sesión: s-20260824T235504-fde8fd6d
tarea: 15
terminada: 2026-08-25T01:07:00Z
HEAD: b62f941 (main = origin/main)

## Recuento
2 ficheros modificados: `editor.html` (9 `<script>` sin cadena `?v=N`) y `sw.js`
(`VERSION` v7 → v8). 10 inserciones / 10 borrados en total.

## Qué se eligió y por qué
Candidata 1 de las tres que dejó la incidencia de la tarea 08: quitar las cadenas
`?v=N` de `editor.html`. Es la más simple, y `VERSION` en `sw.js` ya cumple el mismo
papel de invalidar la caché entera cuando cambia algo; mantener además cadenas de
versión por fichero en `editor.html` era redundante y es lo que causaba el desajuste
con lo que cachea `sw.js` (que nunca las tuvo en cuenta). `index.html` ya no las usaba,
así que ahora los dos ficheros siguen el mismo patrón.

## Verificación
Playwright/Chromium headless contra `http://localhost:8099/`, mismo procedimiento que
usó la tarea 08 para encontrar el fallo:
1. Visita online de `index.html` y `editor.html`: caché `ajedrez-triangular-v8`
   registrada con 22 ficheros.
2. `context.setOffline(true)`.
3. Navegación real (no SPA) a `editor.html`: carga el tablero (1 hijo en `#board-wrap`)
   y el selector de modalidad con sus 9 opciones, 0 errores de consola. Antes del
   arreglo, esto daba 9 `Unexpected token '<'` y ambos elementos vacíos.
4. Navegación a `index.html` en el mismo contexto offline: sigue funcionando igual que
   antes (no se ha roto nada), 0 errores.

Script de verificación en el scratchpad de la sesión, descartado al terminar (no
persistido, como en la tarea 11).

## Pendiente
Nada de esta tarea. La candidata 1 no toca `problemas.js` ni ningún fichero que las
tareas 12/13 (en curso) estén usando.
