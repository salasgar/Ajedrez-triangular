sesión: s-20260824T235449-a524a145
tarea: 11
terminada: 2026-08-25T00:03:21Z

Recuento: 1 fichero modificado (script.js), 1 inserción / 1 borrado (una línea).
Commit: main = b102835 (push confirmado: 77d1120..b102835 main -> main,
https://github.com/salasgar/Ajedrez-triangular.git).

## Diagnóstico
El mecanismo de `help-enpassant` (script.js, `!V.pieces.P`) ya funcionaba
bien: en las 4 modalidades PPT (rps, rpsls, rps-rey, rpsls-rey) `V.pieces`
no incluye `P`, así que la captura al paso ya salía oculta. El fallo real
estaba en `help-draws`: su condición solo miraba `V.kingless`, así que en
`rps-rey` y `rpsls-rey` (con rey, pero sin peones) mostraba «...tras 50
jugadas de cada bando sin capturas ni movimientos de peón», mención de peón
sin sentido en una modalidad que no tiene peones.

## Arreglo
El texto de tablas para las modalidades con rey ahora añade «ni movimientos
de peón» solo si `V.pieces.P` existe. `rps`/`rpsls` (kingless) no se
tocan: siguen con su propio texto de fin de partida.

## Auditoría del resto de puntos de ayuda por modalidad (sin cambios)
- `help-castling` y `help-check`: ya condicionados correctamente
  (`V.castling`, `V.kingless`); comprobado en las 4 PPT.
- Promoción / doble avance de peón: no aparecen en `V.help` de ninguna
  modalidad PPT porque `P` no está en sus `pieces`; nada que ocultar.
- Línea propia sobre quién captura a quién en PPT: ya existe en `V.note`
  de las 4 modalidades (piedra/papel/tijera, con/sin rey); no hacía falta
  añadir nada.

## Verificación
Chromium headless (Playwright, script propio en el scratchpad de la sesión,
descartado al terminar) sobre las 5 modalidades: rps, rpsls, rps-rey,
rpsls-rey y salas (control con peones). Antes del arreglo, `help-draws`
mencionaba peón en rps-rey/rpsls-rey; después, no. `salas` no cambia
(sigue mencionando peón, que sí tiene). `help-enpassant` oculto en las 4
PPT y visible en salas, en las dos pasadas.
