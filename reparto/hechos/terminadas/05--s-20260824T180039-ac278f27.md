sesión: s-20260824T180039-ac278f27
tarea: 05
terminada: 2026-08-24T20:50:00Z

La pestaña «Problemas» está integrada y publicada en `main`:

- `index.html`: los cuatro cambios de la especificación (enlace a `problemas.css`,
  barra de pestañas `#tabs`, `<aside id="panel-problemas">` con los 19 nodos `prob-*`,
  y el orden de carga `problemas.js` ANTES de `script.js`, `problemas-ui.js` DESPUÉS).
- `script.js`: los tres ganchos con guarda `typeof` (bloqueo en `onCellClick`, desvío
  a `problemaTrasJugada()` en las dos ramas que mueven, `probPinta()` al final de
  `render()`).
- **Un fallo encontrado y arreglado que no estaba en la especificación**: `style.css`
  da a `#panel` `display: flex`, que pisa la regla `[hidden]` del navegador; al
  cambiar de pestaña el panel de la partida no se ocultaba. Arreglado en
  `problemas.css` con `#panel[hidden] { display: none; }` (mismo patrón que su regla
  gemela para `#panel-problemas`; `style.css` sigue sin tocarse).

Verificado en Chrome headless sobre http local (capturas en el scratchpad de la
sesión, consola limpia de errores de página):
- La pestaña se activa, los paneles se intercambian, y el panel muestra «Buscando un
  problema…» con el worker preparando el almacén en segundo plano.
- El botón «Imagen (.png)» de `problema-imagen.js` se montó solo entre «Exportar» e
  «Importar», sin tocar `problemas-ui.js` — la integración de problemas-imagen que
  faltaba.
- `node --check script.js` en verde.

Resolver un problema de cada tipo en navegador queda donde el reparto lo asigna: la
tarea 08. Las tareas 06 y 07 quedan PENDIENTES.
