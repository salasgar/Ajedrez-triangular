sesión: s-20260824T235458-3ccd1290
tarea: 12

# Trampas descubiertas trabajando la 12

- **El diseño ya era correcto por construcción, y la investigación empírica lo
  confirma.** `probGeneraUno` (generador) y `probCreaBusca` (crear-problema.js,
  creación manual) ya buscaban «de menos a más jugadas y se quedan con la
  primera que funciona» desde el commit original del motor (`e1e0182`, antes
  de que empezara este reparto). `probJuzga` (problemas-ui.js) ya hace una
  búsqueda Y/O FRESCA tras cada jugada del que resuelve — no compara contra
  la línea guardada. No hacía falta rediseñar el criterio, solo comprobarlo.

  Antes de tocar nada, generé y comprobé más de 500 problemas frescos
  (fácil/medio/difícil, los cuatro tipos) buscando un atajo más corto que el
  declarado (bug 1) y una jugada alternativa que la búsqueda fresca confirma
  como ganadora pero que la propia `probJuzga` rechaza (bug 2, probado tanto
  en la primera jugada como recursivamente en profundidad, siguiendo la
  respuesta que el juego elige para el defensor). **Cero fallos.** Los
  scripts quedan en `entrenamiento/repro-tarea-12*.js` (no se han integrado
  en la suite fija porque no reprodujeron ningún caso: sirven de evidencia y
  de plantilla para reabrir la investigación si el bug reaparece).

- **El bug real que SÍ encontré, por lectura de código, no por generación
  fresca**: `crear-problema.js` verificaba un problema hecho a mano con
  `PROB_CREA_TOPE = 1 200 000` nodos, pero la partida en vivo reverificaba
  cada jugada con `PROB_TOPE_VIVO = 400 000` (problemas.js). Un problema
  manual que costara entre 400 001 y 1 200 000 nodos demostrarse forzado se
  aceptaba al crearlo y luego, al jugarlo, la búsqueda fresca de la primera
  jugada agotaba el presupuesto antes de concluir: `probJuzga` lanzaba
  `PROB_ABORTO`, y `problemaTrasJugada` (problemas-ui.js) retiraba la jugada
  con «no he podido comprobar esa jugada a fondo» — que desde fuera se ve
  igual que «se la da por mala» aunque el mensaje sea distinto del de un
  fallo confirmado. Encaja con el síntoma 2 que reportó Juan Luis. Arreglado
  subiendo `PROB_TOPE_VIVO` a 1 200 000 y hacienda que `PROB_CREA_TOPE` sea
  literalmente `PROB_TOPE_VIVO` (no dos números que hoy coinciden), para que
  el invariante «lo que se acepta al crear se puede reverificar al jugar» no
  se pueda romper subiendo uno sin el otro. No pude reproducir esto con un
  caso concreto (no tengo cómo dibujar en el editor desde Node ni acceso al
  navegador de Juan Luis para fabricar una posición cara de verdad), así que
  quedó documentado por análisis, no por repro numérica.

- **No hay ningún almacén de problemas exportado en el repo.** El «almacén
  precalculado» vive en el `localStorage` del navegador de Juan Luis, al que
  esta sesión no tiene acceso (sin Playwright con su perfil real, que no
  está disponible aquí). No pude re-verificar SU almacén concreto. En su
  lugar:
  1. Confirmé que el único camino que puebla el almacén en vivo es
     `probAlmacenMete(p)`, alimentado exclusivamente por `probGenera` (el
     generador ya probado sin fallos): nada entra ahí sin pasar por el
     bucle mínimo-primero.
  2. El único camino que SÍ puede colar un problema ajeno al criterio es
     importar un `.json` externo (`probFileEl` en problemas-ui.js). Añadí
     `probVerificaForzado(p)` (problemas.js) — repite la búsqueda Y/O para
     confirmar que el enunciado es cierto y mínimo — y la enganché como
     comprobación adicional al importar (`probValidaImportado`, antes solo
     miraba la forma con `probValida`). `probValida` (forma, barata) sigue
     igual; no se puede meter la comprobación de fondo en la carga del
     almacén completo al abrir la pestaña porque, con hasta 8 problemas por
     nivel y modalidad, cada uno con un tope de hasta 400 000-1 200 000
     nodos, congelaría la pestaña al abrirla — se documenta aquí para que
     nadie lo intente sin volver a medir antes.
  3. Si Juan Luis tiene problemas guardados de antes de este reparto que le
     dan el bug 1 o el bug 2 EN VIVO, lo más simple es que los reexporte a
     `.json` y los reimporte: con el fix, la importación ahora los rechaza
     si no se sostienen («o está dañado, o el enunciado no se sostiene»).

- **`git status` no traía hunks sin commitear en problemas*.js** al empezar
  esta tarea (la incidencia de la sesión montadora avisaba de que podía
  haberlos): el árbol estaba limpio para esos ficheros. No hizo falta
  investigar autoría de nada.

- Nodo/`eval` de un solo `require` conjunto (patrón de `prueba-problemas.js`)
  no admite declarar `const N` en el script propio: `N` ya es un global que
  usan `problemas-ui.js`/el worker (tamaño del tablero). Usar otro nombre
  (`MUESTRAS`) si se escribe un script nuevo con el mismo patrón de carga.

- `probGeneraUno(nivel, tipos)` es UN intento (puede devolver null sin más);
  el que reintenta con presupuesto de tiempo es `probGenera(nivel, tipos,
  msTope)`. Para medir el coste de UN intento hace falta `probGeneraUno`
  directamente; para conseguir muestras de verdad, `probGenera`.
