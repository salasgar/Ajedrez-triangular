sesión: s-20260824T180039-ac278f27
tarea: 06
parada: 2026-08-24T21:35:00Z
parada por: sesión agotada

Juan Luis pidió el traspaso con la tarea a medias; no hay nada averiado. Lo hecho está
commiteado y pusheado en `main` = `2881552`:

- `probTiposNivel()` en `problemas.js`: tablas fuera de Difícil y Experto.
- `probRepon()` pide el tipo con menos ejemplares (fin del almacén copado de tablas).
- El selector deshabilita los tipos que el nivel no ofrece (`probAjustaTipos()`).
- `entrenamiento/rendimiento.js` y `entrenamiento/diagnostico.js` reconstruidos y
  probados (fácil/mate: ~297 problemas/min).

Lo que queda, por orden:

1. **Ajustar `PROB_NIVELES` hasta que Difícil y Experto generen.** Medido el
   2026-08-24 tras `probMaxSoluciones`:
   `node entrenamiento/rendimiento.js --nivel=dificil,experto --ms=12000` →
   **0 problemas en las 6 celdas** (mate/gana/corona × 2 niveles). La relajación de
   unicidad NO bastó. El traspaso de problemas ya dejaba dicho el siguiente paso:
   bajar `escapes` o subir `tope` en `PROB_NIVELES` (problemas.js:94), y volver a
   medir. Prueba también con `--ms=30000`: puede que 12 s simplemente sea poco para
   un mate en 3 (medio daba 11 mates por 12 s; dificil siempre midió 0).
2. **Reconstruir `entrenamiento/prueba-problemas.js`** (el verificador independiente
   que recorre el árbol con las reglas, sin el buscador; el original se perdió con el
   scratchpad). Para mate puede hacerse puro sobre `rules.js` con
   `applyEdit`/`makeMove`/`undoMove`/`movesForSide`; para gana/corona/tablas, valorar
   si compensa la independencia total o basta re-verificar con `probSoluciones`.
3. Verificar en navegador que el selector deshabilita «Salvar unas tablas» al elegir
   Difícil/Experto (puede caer en la 08, que ya abre el navegador).

Idempotencia: los puntos hechos no hay que repetirlos; empezar directamente por el 1.
