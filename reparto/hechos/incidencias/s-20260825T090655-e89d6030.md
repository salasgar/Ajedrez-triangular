sesión: s-20260825T090655-e89d6030 (tarea 10)
2026-08-25T12:30Z

Dos hallazgos al aplicar la ronda 15, ninguno de coordinación de este reparto:

1) `./instalar-servicio.sh resultados` lee de
   `~/Library/Application Support/ajedrez-triangular-entrenamiento/r15/`
   (CASA), y ese directorio tiene un corpus más pequeño y más antiguo
   (terminado 2026-08-03 madrugada, ~216.000 posiciones) que el que quedó
   commiteado en el repo por la tarea 01 (terminado 2026-08-03 noche/04
   madrugada, ~350-390.000 posiciones — casi el doble de partidas). Los
   candidatos de la regresión son distintos entre los dos corpus. No sé por
   qué CASA no se sobrescribió con la segunda tanda; no lo investigué más
   porque no es de esta tarea. Para decidir usé SIEMPRE los ficheros del
   repo (`entrenamiento/r15/*.log` y `ajuste-*.txt`), re-corriendo
   `cat entrenamiento/r15/<rival>-[0-9]*.log | node analiza.js 300`
   directamente en vez de fiarme del resultado de `resultados`. Aviso para
   quien use `resultados` en el futuro: comprobar que CASA y el repo están
   de acuerdo antes de fiarse del informe agregado.

2) Bug real ya corregido: el commit `c388624c` (2026-08-04, antes de este
   reparto) aplicó a `variants.js` el candidato "all" de Trigonal de la
   ronda 15 pese a que PERDÍA en su propia arena. Su mensaje de commit cita
   el mismo número que yo obtengo, "elo(A-B): 76 [48, 104]", y lo interpreta
   como que el candidato ganó +76 elo. La convención del script
   (`entrenamiento/analiza.js`+`elo.js`) es `elo(A-B)` = cuánto más fuerte es
   A que B; A es siempre la config vigente. Con A por delante (elo positivo,
   IC fuera de cero) es el candidato B el que pierde — y el marcador en bruto
   lo confirma sin necesidad de fórmula: "A: +194 =127 -103 score 60.7%", A
   por encima del 50%. Llevaba tres semanas en `main` sin que nadie lo
   notara porque un valor de pieza mal puesto no rompe nada, solo hace que
   el motor juegue peor. Revertido en el commit de esta tarea (main =
   56917bf). Dejo aviso por si el mismo despiste está en otro sitio: no
   encontré ningún otro commit con "elo(A-B)" ni "ronda 15" en el mensaje
   aparte de éste, así que no debería haber más casos, pero no lo he
   comprobado exhaustivamente fuera de variants.js.
