sesión: s-20260824T180039-ac278f27
fecha: 2026-08-24T18:35:00Z

1. **Séptimo trabajo sin traspaso, descubierto y commiteado en la 01.** Los hunks de
   `editor.js` (los 7), el hunk 1 de `editor.html` (paleta antes del tablero) y los
   hunks 2-3 de `style.css` forman un trabajo coherente: paleta del editor a la
   izquierda en dos columnas, iconos SVG en los botones, guardas de inicialización y el
   renombrado `onVariantChange` → `aplicarModalidad` (evita un bucle de pila con el
   gancho global de variants.js). Encaja con los restos del worktree
   `fix-position-editor`. Commiteado como `0f91612` en `vaciado-arbol`. Sin traspaso
   propio: la verificación en navegador del editor (tarea 08, punto 2) lo cubre de
   rebote.

2. **La ronda 15 del entrenamiento ha cerrado** (`entrenamiento/r15/dekle-hecha.txt`,
   `salas-1998-hecha.txt`, `trigonal-hecha.txt`). La memoria del proyecto dice qué
   hacer al cerrarse; se añade como tarea 10 al final de la numeración.

3. **`entrenamiento/libro-salas-v4.json` existe en disco pero no salía en `git status`**
   del inventario. Se añadió al commit de entrenamiento con `git check-ignore` de
   guarda. Sin explicación clara; anotado por si reaparece.

4. Trampas de entorno para las demás sesiones: el `sleep` en primer plano está
   bloqueado por el harness (usar `run_in_background` o Monitor); `du` marca 0B en
   ficheros evictados por iCloud aunque `ls -l` dé el tamaño real (el repo vive en
   Documents, que sincroniza).
