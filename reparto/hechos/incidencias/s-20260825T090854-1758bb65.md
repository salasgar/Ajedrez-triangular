sesión: s-20260825T090854-1758bb65
2026-08-25T09:08:54Z

Al reclamar tarea de banda MEDIO, guardé mi sid recién generado en
/tmp/sid_reparto.txt (nombre genérico, fuera de reparto/ y fuera del scratchpad
aislado de esta sesión) para reutilizarlo en un segundo comando. Entre la escritura y
la lectura, otra sesión activa en esta máquina (real, sid s-20260825T090706-b85c3e30,
que reclamó legítimamente la tarea 13 con ese sid — ver
hechos/reclamos/13--s-20260825T090706-b85c3e30.md) sobrescribió ese mismo fichero de
/tmp con su propio sid. Mi segundo comando leyó ese valor ajeno y lo usó para escribir
hechos/reclamos/10--s-20260825T090706-b85c3e30.md, un reclamo de la tarea 10 que en
realidad no es de esa sesión: el contenido es mío, el nombre no.

Corrección: añadida una línea ABANDONADA a ese fichero explicando que es inválido y
remitiendo aquí. La tarea 10 sigue reclamada de verdad por
10--s-20260825T090655-e89d6030.md (abierto 09:06:55Z, antes que mi intento), así que
la cedo sin más: esa sesión llegó primero. Mi sid real y verificado, generado sin
pasar por ningún fichero compartido, es s-20260825T090854-1758bb65.

Lección para el tablón: /tmp no es un sitio seguro para guardar el sid entre dos
llamadas de shell aunque el nombre parezca único, porque varias sesiones de este
reparto corren en la misma máquina y pueden elegir el mismo nombre. El sid hay que
generarlo y usarlo en el mismo comando, o guardarlo dentro de reparto/hechos/ (que sí
lleva el sid en el nombre) o en el scratchpad aislado de la propia sesión.

---

sesión: s-20260825T090854-1758bb65
2026-08-25T18:20:00Z

Segunda incidencia de esta sesión, ya trabajando en la tarea 14 (cosecha de
problemas): el primer script de cosecha se disparó de verdad, no fue un simple
"tarda mucho". Con el presupuesto de nodos del generador (`PROB_TOPE`≈150 000) y el
filtro barato de `probPosicion` ("pieza a tiro", sin mirar si hay defensor) aplicado
tal cual sobre posiciones de partida real, una sola partida de prueba (5 partidas
pedidas, modalidad `salas`) llevaba más de 100 minutos de CPU al 99% sin terminar
la primera. Maté el proceso (PID 12884) en cuanto una comprobación de `ps`
mostró ese tiempo — no había ningún dato en juego, era mi propia prueba, pero
"comer CPU del Mac sin avisar" es justo lo que la ficha de la tarea pide evitar.

Diagnóstico: en una posición de partida real casi cualquier pieza desarrollada
está "a tiro" de algo (no significa nada, a diferencia de las posiciones que arma
el generador con la pieza objetivo ya aislada), así que el filtro dejaba pasar la
búsqueda cara —Y/O completa, hasta 4 profundidades— en casi cada jugada. Y dentro
de esa búsqueda, `probSaldoQuieto` (quiescencia recursiva de `probVeredicto`) NO
cuenta contra el tope de nodos, así que un solo intento podía tardar decenas de
segundos por su cuenta sin que el tope lo cortara.

Arreglo, sin tocar `problemas.js` (nada de esto exigía tocarlo): filtro de "pieza
colgada" (atacada y SIN defensor, no solo "a tiro"), presupuesto de cribado bajado
de 150 000 a 6000 nodos, y un límite de RELOJ nuevo (no de nodos) de 3 s por jugada
de partida que impide abrir intentos nuevos aunque el de nodos no haya bastado.
Con esto, una tanda real de 15 partidas (1380 jugadas) tardó unos 35 minutos de CPU
y no volvió a dispararse. Detalle completo y la cifra exacta de la prueba que lo
detectó, en `hechos/terminadas/14--s-20260825T090854-1758bb65.md` y en las
«Trampas conocidas» de `tareas/tarea-14-cosecha-de-partidas.md`.

Nada que anular ni que decidir Juan Luis aquí: es un fallo de rendimiento en un
script nuevo de esta misma tarea, encontrado y corregido dentro de ella, sin haber
guardado nunca un problema mal verificado (el almacén cosechado no existía todavía
cuando ocurrió).
