sesión: s-20260825T085457-e6fe09b4
tarea: 09
fecha: 2026-08-25T08:58:00Z
incidencia: scripts/nueva-sesion 2.sh tiene cambios reales respecto al original

El fichero «scripts/nueva-sesion 2.sh» es una versión alternativa de «scripts/nueva-sesion.sh»
con un cambio de implementación:

ORIGINAL (scripts/nueva-sesion.sh, líneas 61-65):
```
# Crear rama y worktree de una vez (git worktree add -b crea ambos)
if ! git worktree add -b "$NOMBRE" "$WORKTREE_PATH" main; then
  echo "❌ Error al crear el worktree/rama."
  exit 1
fi
```

ALTERNATIVA (scripts/nueva-sesion 2.sh, líneas 61-66):
```
# Crear rama a partir de main
git checkout -b "$NOMBRE" main

# Crear worktree
git worktree add "$WORKTREE_PATH" "$NOMBRE"
```

La « 2» sustituye una orden atómica (`git worktree add -b`) por dos órdenes secuenciales
(checkout + worktree add). Sin revisión de criterio (no es tarea BAJO consolidar contenido
divergente), NO se movió a papelera.

**Decisión pendiente de Juan Luis:** ¿mover a papelera esta variante, consolidar una de las
dos versiones en el original, o dejar ambas?
