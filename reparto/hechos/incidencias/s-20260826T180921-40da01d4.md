sesión: s-20260826T180921-40da01d4 (tarea 22, banda ALTO)
fecha: 2026-08-26T18:33Z

# El commit de cierre de la 21 quedó huérfano en el índice; lo materializo tal cual

## Lo que encontré

Al reclamar la tarea 22 (18:09Z), la 21 estaba LISTA en `hechos/` (terminada de
las 18:05Z) pero su registro decía «main = PENDIENTE», y en efecto:

- HEAD seguía en `5e69f8e` (el cierre de la 18);
- todos los cambios de la 21 —`ai.js`, `ai-async.js`, `test-ia-rps.js`, los
  bancos y logs `t21-*` de `entrenamiento/`, su terminada, su reclamo, el
  tablón regenerado— más los de la sesión de consulta `09708b47` (incidencia,
  ficha de la tarea 22) estaban **preparados en el índice** (`git add` hecho)
  pero **sin commitear**;
- sin `index.lock` ni ningún proceso git corriendo.

Esperé de 18:11Z a 18:30Z (tres comprobaciones, git aquí tarda 1-3 min por
orden) por si el commit estaba en cola de la sesión que cerró la 21. A las
18:30Z comprobé `.claude/sesiones/`: solo queda mi sesión. **La sesión que
cerraba la 21 terminó sin llegar a commitear.**

## La decisión y el porqué

Materializo el commit huérfano TAL CUAL está preparado: `git commit` a secas,
sin ningún `git add` mío, para que entre exactamente la instantánea que esa
sesión preparó — ni un hunk más ni menos. Mis ficheros (reclamo de la 22,
`t22-conversion--<mi sid>.js`) están sin trackear y quedan fuera; la edición
sin preparar de `tareas/tarea-20-*.md` (de la sesión de consulta) queda en el
árbol, también fuera.

Por qué no esperar más: mi tarea (la 22) escribe en `ai.js` ENCIMA de lo que
dejó la 21. Con su commit en el limbo, mis hunks y los suyos se mezclarían en
el primer commit que alguien hiciera — exactamente el choque que documenta la
incidencia `s-20260824T235458-3ccd1290`. Commitear su índice primero separa
limpiamente su trabajo (commit propio, atribuido) del mío (los cambios que
vienen).

Esto no es editar ficheros de otra sesión: el contenido del commit es
íntegramente el que esa sesión preparó y declaró en su terminada («commit a
main y push» era su paso 7). Es el mismo espíritu que relevar una sesión
caída: comprobar qué dejó hecho y continuar desde ahí sin rehacer ni deshacer.

El mensaje del commit deja constancia de la autoría real (sesión
`s-20260826T095832-0470517d`) y de que lo materializó esta sesión.

# Segunda incidencia (21:50Z): variants.js cambió en mitad de mi sesión, fuera del harness

A las 21:17:54Z (mtime), con mi sesión en plena tarea 22, `variants.js` cambió
en el árbol de trabajo (75 inserciones, 61 borrados, sin preparar): retira las
modalidades sin rey `rps` y `rpsls` y renombra sus posiciones a «Muralla de
papel». El comentario nuevo firma la decisión como de Juan Luis (26-8-2026) y
coincide con el aparcamiento que ya recogía el tablón. En `.claude/sesiones/`
solo está mi sesión: la edición se hizo fuera del harness (VS Code a mano, u
otra herramienta).

Efecto sobre mí: test-ia-rps.js usaba `setVariant('rps')`/`('rpsls')` en ocho
sitios y pasó de verde (19:00Z) a reventar (21:35Z) sin que yo tocara nada de
eso. Resolución elegida (robusta ante ambos futuros de ese cambio):

- Los bloques del modelo ADITIVO pasan a `rps-rey`/`rpsls-rey` (misma matriz)
  con `PROPORCIONAL: 0` explícito.
- Los bloques del soporte kingless y el match dinámica-plana quedan tras un
  guardia `SIN_REY`: se saltan con aviso mientras no exista ninguna modalidad
  kingless y se reactivan solos si vuelve a haberla.
- `variants.js` NO se toca y NO entra en mi commit: no es mío y puede estar a
  medias. Quien lo hizo tendrá que commitearlo (y pasar por `sw.js`/ayudas si
  procede).

Aviso a Juan Luis dado en la propia conversación de esta sesión.
