sesión: s-20260825T093251-fde516e1 (coordinadora, banda ALTO)
2026-08-25T09:33Z

CORRECCIÓN (09:40Z) sobre lo escrito abajo a las 09:33Z: el fichero NO apareció
después del cierre de la 09. La 09 lo dejó a propósito por tener cambios reales
(su firma solo cubría retirar los idénticos) y lo documentó en
`incidencias/s-20260825T085457-e6fe09b4-duplicado.md`, que deja la decisión a
Juan Luis. Esta incidencia añade a aquella un dato de criterio:

- `scripts/nueva-sesion 2.sh` (sin tracking en git, fecha interna 4-8-2026)

NO es idéntico al original: es una versión ANTIGUA de `scripts/nueva-sesion.sh` que
crea la rama con `git checkout -b` sobre el árbol compartido — exactamente la
operación prohibida por las reglas del repo (proyecto.md). El script actual lo hace
bien (`git worktree add -b`, sin tocar el árbol). No hay nada que consolidar: el
original ya lo supersede entero.

RESUELTO (09:42Z): Juan Luis firmó una casilla nueva en `autorizaciones.md`
(25-8-2026) autorizando retirarlo, y esta sesión lo movió a
`reparto/_papelera/nueva-sesion 2.sh`. La papelera la vacía él desde el Finder,
como siempre.

---

SEGUNDA ANOTACIÓN (17:30Z) — desfase horario en los rastros de la sesión
s-20260825T090854-1758bb65 (tarea 14): su terminada dice `cerrado:
2026-08-25T18:20:00Z` y el tablón que regeneró dice `Regenerado: 18:20Z`, pero
los ficheros se escribieron a las 16:54-16:57Z reales (`date -u` de esta
coordinadora): escribió la hora LOCAL (UTC+2) con sufijo Z. Sus latidos de
reclamo (09:09, 14:54) sí iban en UTC correcto; solo el cierre va desfasado.
Sin consecuencia hoy (la 14 no tiene rastros rivales y el orden no cambia),
pero quien compare fechas internas alrededor de ese cierre debe saber que
~2 h de ese sello son ficticias. El trabajo de la 14 en sí está verificado y
correcto.

---

TERCERA ANOTACIÓN (20:57Z) — rescate del scratchpad de la sesión de la 13
(s-20260825T090706-b85c3e30, sesión de VS Code 2645ba0b): sin latidos desde
las 14:17Z, sin escrituras desde las 14:14Z reales y sin ningún proceso node
de cálculo vivo, todo apunta a sesión caída con el diagnóstico a medio camino.
Su reclamo sigue vivo hasta las 03:17Z y NO se releva aquí. Como su material
de trabajo vivía en su scratchpad de /tmp (que se purga a medianoche), esta
coordinadora COPIÓ (sin tocar el original) los 17 ficheros —repro-13.js a
repro-13n.js, hallazgos-13j.json, hallazgos-13k.json y posicion-ply16.json— a
`reparto/salidas/13-rescate-scratchpad-s-20260825T093251-fde516e1/`. Es
material de diagnóstico en bruto para quien continúe la 13; los dos
`hallazgos-*.json` y `posicion-ply16.json` (la posición donde la búsqueda
profunda invierte el veredicto estático) son probablemente lo más valioso. Si
la sesión original revive, su copia de /tmp sigue intacta y manda la suya.
